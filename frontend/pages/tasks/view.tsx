import React from 'react';

const ViewTasks: React.FC = () => {
    return (
        <div>
            <h1>View Tasks</h1>
            <p>This is the page where tasks will be displayed.</p>
        </div>
    );
};

export default ViewTasks;